# 2026-06-27 源码精读日

## 已完成
- 精读 `retrieval/rag_retriever.py`（530行）：完整 pipeline 理解，包括 _merge_results / _rerank / _dynamic_k_from_scores / truncate_to_tokens
- 精读 `chunking/router/content_analyzer.py`：分块路由策略
- 精读 `services/rag_service.py`（415行）：retrieve_context 七步流程，定位 Letta 3个注入点
- 扫读 `models/rag.py`：EvidenceItem 核心数据结构
- 扫读 `routers/chat.py` 第739-887行：入口 + context 注入点
- 写 Obsidian 笔记：`retrieve_context 完整流程串讲.md`

## 关键发现
- `_merge_results_score_boost` 是 toy logic，量纲不匹配（cosine + BM25 raw score）
- RRF 用 rank 替代 raw score，是推荐方案
- `neighbor_window = int((0 or 1))` 写死为1，不可配置
- Letta 注入点：retrieve_context 第339行（追加 evidence） / chat.py 第779行（context 字典）

## 代码清理
- `OllamaService` → `LLMService`（8个文件，清除 Ollama 命名遗留）
- 删除 `services/model_selector.py`（死代码，model_selector 默认情况下三路都走 LLM_MODEL）
- 删除 `general_assistant` 和 `physics_assistant` 中的模型切换逻辑（不会触发）

## 晚间
- 用户自行精读 MemGPT 论文，写了完整架构总结 → 写入 Obsidian `个人学习笔记/advanced-rag/MemGPT 架构理解总结.md`
