# 2026-07-20 工作日志

## 面试模拟：PAOR 循环与检索架构讲解

- 深度走读 `agents/general_assistant/general_assistant_agent.py`、`services/llm_service.py`、`services/evidence_verifier.py`、`services/query_planner.py`，完整梳理 Plan-Reasoning-Act-Observe-Reflect 循环
- 确认前置 Plan（QueryPlanner）已接入对话路径，支持 intent 驱动的条件预检索
- 区分 QueryPlanner（前置结构规划）与 LLM Function Calling（逐轮 Reasoning）的两个"规划"概念
- 产出完整 PAOR 循环自然语言描述 + 8 段证据代码

## 混合检索 RRF 与 CrossEncoder

- 讲解 RRF 三路不对称权重（vector=1.0 / keyword=0.3 / graph=0.2）的消融实验依据
- 等权 RRF recall@5=0.4733 vs 加权 0.6333，差 16pp
- CrossEncoder 精排：RRF 截取 top-20 → bge-reranker-base → 动态 K 裁剪

## 92.5 倍 BM25 性能提升

- 来自 150 题完整消融：MongoDB + regex 候选（1138.1ms）→ RediSearch（12.3ms）= 92.53x
- 数据规模：9710 chunks，7.9MB 文本

## 三厂商 Function Calling 适配层

- 四层架构：Canonical 数据层 → 探测层 → 转换层 → 门面层
- 核心差异：arguments string/dict、流式格式（delta vs 三阶段 vs 完整对象）、工具结果 role
- 写入 Obsidian：`个人学习笔记/开发经验/2026-07-20 advanced-rag 三厂商Function Calling适配层设计.md`
