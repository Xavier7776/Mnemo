# 2026-06-26 Phase 3 Day 1：advanced-rag 项目搭建 + Mimo API 改造

## 完成项

### 代码改造（Ollama → OpenAI/Mimo API）
- `services/ollama_service.py`：重写为 OpenAI 兼容格式（openai 库 + chat/completions）
- `embedding/embedding_service.py`：重写为 fastembed 本地模型（无 torch）
- `services/model_selector.py`：去除 Ollama 直接调用
- `services/title_generator.py`：去除 Ollama 直接调用
- `services/query_analyzer.py`：去除 Ollama 直接调用
- `services/knowledge_extraction_service.py`：去除 Ollama 直接调用
- `services/ai_tools.py`：替换 ollama 模型列表查询
- `eval/evaluate.py`：替换 Ollama 评估调用

### 配置文件
- 创建 `.env.development`（Mimo API + Docker 服务地址）
- 修改 `requirements.txt`（sentence-transformers → fastembed）

### 基础设施
- Docker Compose 启动 4 个服务（MongoDB/Qdrant/Neo4j/Redis）

### 未完成
- venv 依赖尚未安装完成（用户手动执行 pip install）
- 后端尚未启动验证
